# FitArena Frontend

This is the FitArena Next.js frontend.

Quick start

```bash
# install dependencies
npm install

# run development server
npm run dev

# build for production
npm run build

# start production server
npm run start
```

Environment

- Copy `.env.example` to `.env.local` and update values.

Project structure

- `app/` - Next.js App Router pages
- `components/` - UI components
- `hooks/` - custom React hooks
- `lib/` - utilities

If you want me to implement specific features or fixes, tell me which ones and I'll continue.